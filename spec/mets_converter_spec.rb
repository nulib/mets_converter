require 'spec_helper'

RSpec.describe MetsConverter do
  it 'has a version number' do
    expect(MetsConverter::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
